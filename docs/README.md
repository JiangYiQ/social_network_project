**README（根据实际情况填写）**

**项目概述**

本项目为数据结构课程设计作品，基于图、哈希表、堆等自主实现的数据结构，开发社交网络图谱分析及智能推荐系统，支持人脉查询、社交距离计算、智能推荐等核心功能，旨在掌握数据结构底层原理与系统开发流程。

**开发语言**： Python 3.7+

**开发团队**： 江依晴-2400130137；李英姿-2400130140

**开发周期**：2025-2026学年第一学期（1周/5个工作日）

环境配置

1.  开发环境

- **IDE**： PyCharm
- **运行环境**： Python 3.14.3
- **依赖库/组件**：
- Python：Tkinter/PyQt（GUI）、Matplotlib/PyVis（可视化，扩展功能用）

1.  依赖安装

Python

bash  
\# 基础依赖（GUI）  
pip install pyqt5  
\# 扩展功能依赖（可视化）  
pip install matplotlib pyvis

文件结构

plain text  
社交网络系统/  
├─ data/ # 测试数据文件  
│ ├─ user_data.csv # 用户信息数据（自定义格式）  
│ └─ friend_relation.txt # 好友关系数据（自定义格式）  
├─ src/ # 源代码目录  
│ ├─ data_structure/ # 自主实现的数据结构  
│ │ ├─ adjacency_list.xx # 邻接表（xx为对应语言后缀，如py/java/cpp）  
│ │ ├─ hash_table.xx # 哈希表  
│ │ └─ heap.xx # 堆（扩展功能用，如Top-K推荐）  
│ ├─ algorithm/ # 核心算法  
│ │ ├─ bfs.xx # BFS算法（人脉查询、距离计算）  
│ │ └─ recommendation.xx # 推荐算法（扩展功能用）  
│ ├─ gui/ # GUI界面  
│ │ └─ main_window.xx # 主界面及交互逻辑  
│ ├─ utils/ # 工具类  
│ │ └─ data_reader.xx # 数据读取（CSV/TXT解析）  
│ └─ main.xx # 程序入口  
├─ test/ # 单元测试（加分项）  
│ └─ test_data_structure.xx # 数据结构测试用例  
├─ README.md # 本说明文档  
└─ 演示视频.mp4 # 项目演示视频（单独提交，此处为占位）

运行步骤

Python

1.  下载源代码包并解压，用PyCharm打开项目目录；
2.  安装上述依赖库；
3.  定位到src/main.py，右键运行即可启动程序。

数据格式说明

系统支持从CSV/TXT文件加载数据，自定义格式如下（可根据需求调整，需与代码解析逻辑一致）：

1.  用户信息（user_data.csv）

plain text  
用户ID,姓名,兴趣标签（扩展功能用）  
1,张三,编程;篮球  
2,李四,阅读;音乐  
3,王五,编程;跑步

1.  好友关系（friend_relation.txt）

plain text  
用户ID1,用户ID2  
1,2  
1,3  
2,3

**说明**：数据文件需放在data目录下，程序启动后可通过GUI“加载数据”按钮选择文件。

功能说明

1.  基础功能（必实现）

- 图结构建模：邻接表实现，支持用户、好友关系增删查改；
- 数据持久化：加载CSV/TXT格式的用户及好友关系数据；
- 一度人脉查询：输入用户ID，返回所有直接好友；
- 二度人脉发现：基于BFS算法，查询好友的好友（排除自身及一度人脉）；
- 社交距离计算：计算任意两用户最短社交距离及路径；
- 用户信息管理：哈希表缓存用户信息，优化查询效率；
- GUI界面：集成所有功能，支持输入校验、结果展示、异常提示。

1.  扩展功能（加分项，已实现： A：多度人脉查询（N≥3）和C：基于兴趣的智能推荐）

（选择实现的扩展功能，简要说明实现内容，如“智能推荐-基于共同兴趣的Top-3推荐”）

注意事项

- 核心数据结构（邻接表、哈希表、堆）均为自主实现，禁止直接调用第三方库（如Python NetworkX、Java HashMap外的图库）；
- 运行前请确保数据文件格式正确，避免解析异常；
- 若扩展功能涉及多线程/数据库，需在环境配置中补充对应依赖；
- 程序异常可查看控制台输出，常见问题：依赖未安装、数据路径错误、环境变量未配置。

团队分工

|     |     |     |     |
| --- | --- | --- | --- |
| **姓名** | **学号** | **负责模块** | **核心贡献** |
| 江依晴 | 2400130137 | 模型层核心开发（算法+后端） | 负责SocialGraph类全部实现：邻接表、哈希表、数据加载、一度人脉、二度人脉（BFS优化版）、多度人脉（扩展功能A，N≥3）、BFS/最短路径算法、基于倒排索引实现扩展功能C；编写单元测试；性能测试与初步优化。 |
| 李英姿 | 2400130140 | 视图层与控制层开发（GUI+集成） | 负责SocialNetworkApp类：基于Tkinter搭建主窗口、菜单栏、左侧功能区和右侧结果显示区；事件绑定、前后端调用；集成模型层代码，完成所有功能按钮的响应；负责项目文档编写、录制演示视频，项目最终演示。 |